import { ChartFrame } from "@/components/charts/ChartFrame";
import { BarChart } from "@/components/charts/BarChart";
import { AreaChart } from "@/components/charts/AreaChart";
import { GroupedBarChart } from "@/components/charts/GroupedBarChart";
import { PullQuote } from "@/components/PullQuote";
import { StatCallout } from "@/components/StatCallout";
import { SectionLead } from "@/components/SectionLead";
import {
  diagnosticSpeed,
  diagnosticSpeedByTool,
  incidentsBy90Days,
  topFailureCauses,
} from "@/content/report";

<SectionLead>
  Engineers using observability dashboards built into their orchestration
  layer report the fastest path to understanding crashes when they occur:
  40% diagnose in minutes, while 1% note lengthy delays. For teams using
  only APM tools, 29% diagnose in minutes, with 11% reporting delays.
</SectionLead>

In this survey we asked one free-text question: *"What is the biggest
unsolved problem in building reliable workflows, agents, and endpoints?"*
One theme dominated the responses: observability.

While not exclusively an AI problem, observability was cited more frequently
by teams building AI in production. This may be because observability tools
were built on the premise that the same inputs produce the same outputs. AI
systems break that assumption. When a workflow fails because an LLM returned
a malformed response at step three of five, the error surfaces as a
downstream consequence in a system that had no reason to expect it.

<PullQuote attribution="Engineer, 2–10 engineer team">
  Observability gaps — when something breaks, it's hard to trace why an
  agent made a decision or reproduce it. Debugging feels more like
  forensics than engineering. LLMs don't crash cleanly; they degrade,
  hallucinate, or partially succeed. That makes it hard to define clear
  failure modes and retries.
</PullQuote>

### What we asked

1. How do you observe workflow runs?
2. When a workflow fails in production, how long does it typically take to
   understand what went wrong?
3. In the past 90 days, has a workflow or background job failure caused a
   customer-visible incident?
4. What are the most common causes of failures? (pick up to 3)

### Observability platforms and diagnostic speed

38% of teams can diagnose a production failure in minutes. Another 54% get
there in under an hour but require active investigation. 9% take hours or
can't fully explain what happened at all.

<ChartFrame
  variant="breeze"
  label="Figure 8"
  caption="Time to understand what went wrong"
  source="Q5: When a workflow fails in production, how long does it typically take to understand what went wrong? All respondents n=130."
  aspect="aspect-[16/8]"
>
  <BarChart data={diagnosticSpeed} valueSuffix="%" horizontal showTrack />
</ChartFrame>

The gap between these cohorts is almost entirely explained by observability
tooling. Platform-native dashboards — where observability is built into the
orchestration layer — deliver the best outcomes: 40% diagnose in minutes,
with only 1% in the slow-or-blind band.

Teams using only APM tools without an orchestration dashboard: 29% fast,
11% slow or blind. That's an 11× difference in worst-case outcomes.

<ChartFrame
  variant="breeze"
  label="Figure 9"
  caption="Time to understand what went wrong — by observability tool"
  source="Q5 × Q4: Respondents may use multiple observability tools. Sorted by % diagnosing in minutes."
  aspect="aspect-[16/9]"
>
  <AreaChart
    rows={diagnosticSpeedByTool}
    keys={["Minutes — good tracing", "Under an hour", "Hours", "Can't explain"]}
    colors={{
      "Minutes — good tracing": "#2C9B63",
      "Under an hour": "#2389F1",
      "Hours": "#FF7300",
      "Can't explain": "#F54A3F",
    }}
    direction={{ left: "← fast", right: "slow →" }}
  />
</ChartFrame>

_Orchestration platform dashboards lead on fast diagnosis (40%) and have the
lowest combined slow/can't-explain rate (5%). Sentry, Datadog, and New Relic
have the highest "hours" rate (7%) despite being the most widely used tools —
broad adoption doesn't mean fast resolution. Teams with no structured
observability (n=7, not shown) split sharply: 43% diagnose in minutes but
29% can't explain at all — either the failure is immediately obvious or
you're completely in the dark._

<StatCallout
  value="11×"
  label="Teams using only APM tools are 11× more likely to spend hours understanding a failure vs teams using orchestration-native insights."
  source="Inngest 2026 Durable Execution Benchmark (n=130)"
/>

<PullQuote attribution="VP of Engineering, 501–1,000 engineer team">
  Apart from unconstrained cost management in our workflows, our biggest
  challenge is observability. We seem to get more and more logic in our
  workflows getting pushed to LLMs, which not only makes them
  non-deterministic but just hard to evaluate for anything other than raw
  response time.
</PullQuote>

> **Key takeaway:** Orchestration tools that provide granular observability
> are the best shot teams have to find what went wrong, quickly, when
> workflows fail.

### Rate and cause of incidents in AI vs non-AI use cases

74% of teams building AI in production experienced at least one
customer-visible incident in the past 90 days — vs 62% of non-AI teams. One
in five AI teams had three or more, while teams larger than 500 engineers
rarely accumulate 3+.

<ChartFrame
  variant="breeze"
  label="Figure 10"
  caption="Customer-visible incidents in the past 90 days"
  source="Q7: In the past 90 days, has a workflow or background job failure caused a customer-visible incident? AI vs. non-AI teams."
  aspect="aspect-[16/7]"
>
  <AreaChart
    rows={incidentsBy90Days}
    keys={["3 or more times", "Once or twice", "No incidents", "Unsure"]}
    colors={{
      "3 or more times": "#F54A3F",
      "Once or twice": "#E5A83E",
      "No incidents": "#2C9B63",
      "Unsure": "#B0B0B0",
    }}
    direction={{ left: "← more incidents", right: "none →" }}
  />
</ChartFrame>

Infrastructure crashes are a top cause of workflow failure for both AI and
non-AI use cases, though LLM / external API failures invert the top slot for
those building with AI.

<ChartFrame
  variant="breeze"
  label="Figure 11"
  caption="What breaks — AI teams vs non-AI teams"
  source="Q6: What are the most common causes of failures? Pick up to 3. % of each group citing each cause."
  aspect="aspect-[16/10]"
>
  <GroupedBarChart
    rows={topFailureCauses.map((d) => ({
      label: d.cause,
      AI: d.ai,
      "Non-AI": d.nonAI,
    }))}
    series={[
      { key: "AI", label: "AI (n=88)", color: "#F54A3F" },
      { key: "Non-AI", label: "No AI (n=42)", color: "#2389F1" },
    ]}
  />
</ChartFrame>

These trends mostly hold by team size, though the largest teams report a
much higher occurrence of infra and scaling-related failures — maybe
unsurprisingly.
