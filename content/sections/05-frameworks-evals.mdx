import { ChartFrame } from "@/components/charts/ChartFrame";
import { BarChart } from "@/components/charts/BarChart";
import { AreaChart } from "@/components/charts/AreaChart";
import { GroupedColumnChart } from "@/components/charts/GroupedColumnChart";
import { HeatmapTable } from "@/components/charts/HeatmapTable";
import { PullQuote } from "@/components/PullQuote";
import { SectionLead } from "@/components/SectionLead";
import {
  evalApproachByTeamSize,
  confidenceByEvals,
  evalGaps,
  evalGapsByTool,
  frameworkAdoptionByTeamSize,
  confidenceByFramework,
  frameworkGaps,
  frameworkGapsByTool,
} from "@/content/report";

<SectionLead>
  While evals are closely tied to confidence in scale, we're still early.
  28% of respondents believe it's hard to write evals that matter, 35%
  aren't using them at all, and most who are, have built their own pipeline.
</SectionLead>

AI frameworks and evals exist in the layer of building AI where
non-determinism becomes harder to manage through better infrastructure alone.

**Evals** aim to measure whether AI outputs are correct, safe, and within
acceptable variance.

**Agent frameworks** are the libraries that sit between application code and
the model — structuring calls, managing context, handling tool use.

Both categories barely existed two years ago and have grown fast. And both
may share a common achilles heel: they were built as standalone layers,
without a native connection to the orchestration layer that knows what
actually ran.

### What we asked

1. What are you using to evaluate AI outputs in production?
2. What do you believe are the biggest limitations, if any, of modern AI
   eval solutions?
3. What, if anything, are you using for an agent framework?
4. What do you believe are the biggest limitations, if any, of modern agent
   frameworks?

### Using evals to improve AI quality in production

Maybe the most surprising finding in this survey is that 35% of respondents
building AI in production are not doing AI evals at all. Of the 65% who are,
most have built their own pipeline.

Tool selection spread seems to hold across all team sizes, though as with
orchestration solutions, teams begin to default to building their own
solution when they're small enough that complexity is low, or big enough
that bespoke solutions seem like the only answer.

<ChartFrame
  variant="matcha"
  label="Figure 13"
  caption="Eval approach by team size"
  source="Q14: AI teams only. % of each size band. Multi-select."
  aspect="aspect-[4/3]"
>
  <GroupedColumnChart
    cohorts={evalApproachByTeamSize.cohorts}
    tools={evalApproachByTeamSize.tools}
  />
</ChartFrame>

<PullQuote>
  The biggest unsolved problem is being able to evaluate complex multi-agent
  workflows quickly and effectively. Evaluating all of these non-deterministic
  intermediary steps is very difficult to do at scale.
</PullQuote>

Growth phase teams (11–50 engineers) exhibit the highest rate of "not doing
evals" at 44%. This may simply be because these teams are moving fast,
shipping fast, and deferring infrastructure investments until something
breaks.

### Confidence in AI stack correlated with eval solutions

However, when looking at confidence in AI stack against the presence of eval
solutions, we see some indicators that this tooling choice may correlate
with overall confidence in scale. Considering flaky LLMs are the #1 driver
of workflow failure for those building with AI, this particular correlation
(confidence and use of evals) may be unsurprising.

<ChartFrame
  variant="honey"
  label="Figure 14"
  caption="Scaling confidence by eval approach"
  source="AI teams only. Very / somewhat / not very confident. * = n<10, directional. Values on the right: very confident % / not confident %."
>
  <AreaChart
    rows={confidenceByEvals}
    keys={["Very confident", "Somewhat confident", "Not very confident"]}
    colors={{
      "Very confident": "#2C9B63",
      "Somewhat confident": "#9ADAB3",
      "Not very confident": "#FA8D86",
    }}
  />
</ChartFrame>

### Perceived opportunities for improvement in eval solutions

Regardless of whether teams invested in, built, or deferred eval solutions,
they also answered what gaps they perceived to exist in the category.

While the top two reasons cited pertain exclusively to the ergonomics of
eval solutions, three are failures of context: results in a separate system
(20%), no way to act on a failed eval (13%), we don't have enough coverage
to trust our output (12%), evals offline and disconnected from production
behavior (10%). Nearly half of all AI teams (47%) cite at least one of
these three. Each describes the same structural absence: eval state and
orchestration state that can't see each other.

<PullQuote>
  Deeply integrated infrastructure, data sources, evaluations, and data
  migrations based on evaluation results — that's what's still missing.
</PullQuote>

<ChartFrame
  variant="breeze"
  label="Figure 15"
  caption="What's wrong with current eval solutions"
  source="Q15: What do you believe are the biggest limitations of modern AI eval solutions? % of all respondents citing each limitation (n=143)."
>
  <BarChart data={evalGaps} valueSuffix="%" horizontal showTrack insideLabels />
</ChartFrame>

To add a little more color, we can compare gaps cited against tools in use,
only to extrapolate potential opportunities for improvement in each. It's
important to note that respondents were not asked about gaps in their
solutions — just in the category overall.

<ChartFrame
  variant="ruby"
  label="Figure 16"
  caption="What's wrong with current eval solutions — by tool"
  source="Q15: % of each tool's users citing each limitation. Red = 50%+, amber = 30–49%."
>
  <HeatmapTable
    columns={evalGapsByTool.columns}
    rows={evalGapsByTool.rows}
    footnote="Tools with n<3 (W&B, Helicone) excluded. “Not doing evals” row included for context."
  />
</ChartFrame>

### Using agent frameworks to guide AI Agent production

<SectionLead>
  69% of respondents building AI in production not using third-party agent
  frameworks, instead choosing to call LLM APIs directly, or build their
  own abstractions. Observability-related fears top the list of perceived
  gaps, with 26% saying abstractions make failures harder to trace.
</SectionLead>

Agent frameworks provide the foundational components to simplify the process
of building, deploying, and managing autonomous AI agents, though the
category is still quite new.

Given the popularity of frameworks like Langchain, it might be surprising
that 69% of respondents building AI just call LLM APIs directly, or have
built their own abstractions.

The remaining 31% are split between Vercel AI SDK (30%),
LangChain/LangGraph (20%), and smaller tools. This trend hold for various
size cohorts, though we do see adoption of Langchain/Langraph skewing
towards larger teams.

<ChartFrame
  variant="blush"
  label="Figure 18"
  caption="Agent framework adoption by team size"
  source="Q16: AI teams only. % of each size band. Multi-select."
  aspect="aspect-[4/3]"
>
  <GroupedColumnChart
    cohorts={frameworkAdoptionByTeamSize.cohorts}
    tools={frameworkAdoptionByTeamSize.tools}
  />
</ChartFrame>

### Confidence in AI stack correlated with framework solutions

When looking at how overall confidence in AI stack correlates with presence
of framework solutions, we see growth-stage tools like Mastra correlating
with higher confidence levels.

<ChartFrame
  variant="matcha"
  label="Figure 19"
  caption="Scaling confidence by agent framework"
  source="AI teams only. Very / somewhat / not very confident. * = n<10, directional. Values on the right: very confident % / not confident %."
>
  <AreaChart
    rows={confidenceByFramework}
    keys={["Very confident", "Somewhat confident", "Not very confident"]}
    colors={{
      "Very confident": "#2C9B63",
      "Somewhat confident": "#9ADAB3",
      "Not very confident": "#FA8D86",
    }}
  />
</ChartFrame>

### Perceived opportunities for improvement in framework solutions

Regardless of whether teams invested in, built, or deferred framework
solutions, they answered what gaps they perceived to exist in the category,
generally. The predominant gaps cited are that agent framework abstractions
make failures harder to trace (26%), and there's poor support for
long-running or stateful workflows (19%).

<ChartFrame
  variant="honey"
  label="Figure 20"
  caption="What's broken about agent frameworks"
  source="Q17: What do you believe are the biggest limitations of modern agent frameworks? % of all respondents citing each limitation (n=143)."
>
  <BarChart data={frameworkGaps} valueSuffix="%" horizontal showTrack insideLabels />
</ChartFrame>

However, it should be noted that fear of making failures harder to trace
while using an agent framework did not shake out in the data. Time to
diagnose workflow failures is nearly identical between those using
frameworks, and those calling LLMs directly without a framework.

To add a little more color to these perceived gaps, however, we can compare
against tools in use, only to extrapolate potential opportunities for
improvement in each. It's important to note that respondents were not asked
about gaps in their solutions — just in the category overall.

<ChartFrame
  variant="breeze"
  label="Figure 21"
  caption="What's broken about agent frameworks — by tool"
  source="Q17: % of framework users citing each limitation. Red = 50%+, amber = 30–49%."
>
  <HeatmapTable
    columns={frameworkGapsByTool.columns}
    rows={frameworkGapsByTool.rows}
  />
</ChartFrame>
